apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
spec:
  selector:
    app: mmli-fastapi
  type: {{ .Values.service.type | default "ClusterIP" }}
  ports:
    - protocol: "TCP"
      port: 8080
      targetPort: 8080
