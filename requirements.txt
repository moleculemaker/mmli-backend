fastapi[all]==0.115.6

uvicorn==0.34.0
pandas==2.0.3
python-dotenv==1.0.1
minio==7.1.17
rdkit==2023.3.3
requests==2.31.0
kubernetes==34.1.0

# For ReactionMiner
python-terrier==0.13.0
safetensors==0.5.2

# Pin dependencies to specific versions
# NumPy 2.0.0 is not backwards compatible
numpy==1.26.4

# ORM layer
sqlmodel==0.0.22
asyncpg==0.28.0
alembic==1.11.1
SQLAlchemy==2.0.36
pydantic==2.10.4

psycopg2-binary==2.9.9

# Validates job inputs against the published per-tool JSON Schema 2020-12 documents
# in app/schemas/. Pydantic cannot be used for this: those schemas are authored as
# standalone artifacts for external consumers, not generated from models.
jsonschema==4.23.0

# Model Context Protocol server, exposing the same tools to AI agents.
#
# starlette is pinned explicitly alongside it. It is normally a transitive dependency of
# fastapi, which constrains it to <0.42; the mcp package has no such upper bound, so an
# unpinned resolve upgrades starlette past what fastapi supports and breaks the whole
# application. Keep this pin inside fastapi's supported range when bumping either.
mcp==1.12.4
starlette==0.41.3
