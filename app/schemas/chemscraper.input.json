{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ChemScraper input",
  "description": "Extracts chemical structures from a PDF. Upload the PDF first; input_file names it.",
  "type": "object",
  "required": [
    "input_file"
  ],
  "additionalProperties": false,
  "properties": {
    "input_file": {
      "type": "string",
      "minLength": 1,
      "description": "Filename of a PDF previously uploaded to this job's input directory.",
      "examples": [
        "paper.pdf"
      ]
    }
  }
}
